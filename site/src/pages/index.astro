---
import Layout from '../layouts/Layout.astro';
import CTASection from '../components/CTASection.astro';
import Icon from '../components/Icon.astro';
import AppIcon from '../components/AppIcon.astro';
import { BUDGETBABY_APP_STORE_URL } from '../data/appStore';
import PhoneMockup from '../components/abstract/PhoneMockup.astro';
import BudgetBabyPreview from '../components/abstract/BudgetBabyPreview.astro';
---

<Layout
  title="BabyBlocks: BudgetBaby and other iOS apps for family life"
  description="BudgetBaby helps you plan bills, spending, and savings without connecting your bank. Built by BabyBlocks, an independent studio."
  canonical="/"
  theme="core"
>
  <!-- ── Hero ─────────────────────────────────────────────────────────── -->
  <section class="hero">
    <div class="container">
      <div class="hero-grid">
        <div class="hero-copy">
          <a
            href="https://intriguinginsight.com"
            target="_blank"
            rel="noopener noreferrer"
            class="lineage-chip"
            data-event="lineage_chip_click"
            data-event-source="hero"
          >
            <span class="lineage-chip-faint">A solution from</span>
            <span class="lineage-chip-strong">Intriguing Insight</span>
            <span class="lineage-chip-sep" aria-hidden="true">·</span>
            <span class="lineage-chip-faint">Intrigue MMC LLC</span>
          </a>
          <div class="brand-row">
            <img src="/icons/budgetbaby-icon.png" alt="" width="40" height="40" class="brand-glyph" />
            <span class="brand-pill">BudgetBaby</span>
            <a
              href={BUDGETBABY_APP_STORE_URL}
              class="brand-status"
              aria-label="Download BudgetBaby on the App Store"
              target="_blank"
              rel="noopener"
              data-event="app_store_click"
              data-event-source="hero"
            >
              <span class="brand-status-dot" aria-hidden="true"></span>Live on the App Store
            </a>
          </div>
          <h1 class="display-serif hero-title">
            Know what's safe<br />to spend today.
          </h1>
          <p class="hero-sub">
            BudgetBaby helps you budget with confidence. Tell it your income,
            your bills, and what you want to save, it gives you one number
            you can actually spend, right now.
          </p>

          <div class="hero-actions">
            <a href={BUDGETBABY_APP_STORE_URL} class="btn btn-dark btn-lg" target="_blank" rel="noopener" data-event="app_store_click">
              Download on the App Store
            </a>
            <a href="/budgetbaby" class="btn btn-secondary btn-lg" data-event="how_it_works_click">
              How it works
            </a>
          </div>
          <p class="hero-launch-note">
            Free on the App Store. Premium is optional.
          </p>

          <!-- Honest status row. No fabricated stars or avatars until there
               are real users to back them up. The single line tells visitors
               what they're looking at: an iOS app, in App Review, with a free
               tier and a paid one. -->
          <ul class="hero-facts" role="list">
            <li>iPhone, iOS 17 and up</li>
            <li>Free to use</li>
            <li>Premium $7.99/mo or $69.99/yr</li>
          </ul>
        </div>

        <div class="hero-visual" aria-hidden="true">
          <div class="phone-stage">
            <div class="phone-glow" aria-hidden="true"></div>
            <div class="phone-block phone-block--peach" aria-hidden="true"></div>
            <div class="phone-block phone-block--blush" aria-hidden="true"></div>

            {/* Back phone. Insights screen. */}
            <div class="phone-slot phone-slot--back">
              <PhoneMockup tone="dark" tilt={-3} width={210}>
                <BudgetBabyPreview variant="insights" />
              </PhoneMockup>
            </div>

            {/* Front phone. Home dashboard. */}
            <div class="phone-slot phone-slot--front">
              <PhoneMockup tone="dark" tilt={2} width={230}>
                <BudgetBabyPreview variant="home" />
              </PhoneMockup>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── Dark feature strip ─────────────────────────────────────────────── -->
  <section class="dark-band feature-strip">
    <div class="container">
      <div class="feature-strip-grid">
        <div class="feature-tile">
          <div class="feature-tile-icon"><Icon name="target" size={22} /></div>
          <h3>Safe to Spend</h3>
          <p>One number that tells you exactly what you can spend today. Rent, bills, and savings are already accounted for.</p>
        </div>
        <div class="feature-tile">
          <div class="feature-tile-icon"><Icon name="bar-chart-2" size={22} /></div>
          <h3>Plan Ahead</h3>
          <p>See your upcoming bills on a calendar. Budget for the month before the month starts, not after it ends.</p>
        </div>
        <div class="feature-tile">
          <div class="feature-tile-icon"><Icon name="lock" size={22} /></div>
          <h3>Trust Everything</h3>
          <p>No bank connection. No data leaving your phone unless you flip on backup. Your finances stay private.</p>
        </div>
        <div class="feature-tile">
          <div class="feature-tile-icon"><Icon name="file-text" size={22} /></div>
          <h3>Built for Parents</h3>
          <p>Designed for busy households. Log an expense in a tap. Back to your day before the baby notices.</p>
        </div>
      </div>

      <div class="dark-trust">
        <div class="dark-trust-rule" aria-hidden="true"></div>
        <p class="dark-trust-line">
          <span class="dark-trust-eyebrow">A few things we will not do</span>
          Plug into your bank. Sell your data. Show you ads. Keep nudging you to upgrade.
          <a href="/about">Why <span aria-hidden="true">&rarr;</span></a>
        </p>
      </div>
    </div>
  </section>

  <!-- ── More from BabyBlocks ────────────────────────────────────────── -->
  <section class="apps-section section-sm">
    <div class="container">
      <div class="apps-section-head">
        <div>
          <span class="eyebrow">More from BabyBlocks</span>
          <h2 class="display-serif">A few more apps in the works.</h2>
          <p>Each app solves one specific daily headache. Grab one, skip the rest, they work independently.</p>
        </div>
      </div>

      <div class="apps-grid">
        <a href="/budgetbaby" class="app-card app-card--featured">
          <div class="app-card-head">
            <AppIcon app="budgetbaby" size={48} />
            <span class="app-status app-status--soon">
              <span class="status-dot status-dot--soon" aria-hidden="true"></span>Join the waitlist
            </span>
          </div>
          <h3>BudgetBaby</h3>
          <p>One number on your home screen tells you what you can spend today.</p>
          <span class="app-card-link">Learn more <span aria-hidden="true">&rarr;</span></span>
        </a>

        <a href="/coming-soon/pantrybaby" class="app-card">
          <div class="app-card-head">
            <AppIcon app="pantrybaby" size={48} />
            <span class="app-status app-status--build">
              <span class="status-dot status-dot--build" aria-hidden="true"></span>Join the waitlist
            </span>
          </div>
          <h3>PantryBaby</h3>
          <p>What is in the cupboard, what you are out of, and what to grab on the way home.</p>
          <span class="app-card-link">Get notified <span aria-hidden="true">&rarr;</span></span>
        </a>

        <a href="/coming-soon/babyblocks-core" class="app-card">
          <div class="app-card-head">
            <div class="app-icon-future" aria-hidden="true">
              <Icon name="box" size={22} />
            </div>
            <span class="app-status app-status--future">
              <span class="status-dot status-dot--future" aria-hidden="true"></span>Join the waitlist
            </span>
          </div>
          <h3>BabyBlocks Core</h3>
          <p>A single home for all the apps. Still on paper. Nothing is being coded yet.</p>
          <span class="app-card-link">Read the idea <span aria-hidden="true">&rarr;</span></span>
        </a>
      </div>
    </div>
  </section>

  <!-- ── Pricing ─────────────────────────────────────────────────────── -->
  <section class="pricing-section section-sm">
    <div class="container">
      <div class="pricing-head section-header centered">
        <span class="eyebrow">Pricing</span>
        <h2 class="display-serif">Same price across the family.</h2>
        <p>BudgetBaby Premium and PantryBaby Premium are priced identically. Buy one or both; each subscription is per-app. Free tiers are real apps, not demos.</p>
      </div>

      <div class="pricing-cards">
        <div class="price-card">
          <div class="price-card-head">
            <span class="price-tier">Monthly</span>
            <div class="price-amt-row">
              <span class="price-amt">$7.99</span>
              <span class="price-period">/month</span>
            </div>
            <span class="price-save">per app</span>
          </div>
          <ul class="price-features">
            <li>Everything in the free tier</li>
            <li>Unlimited custom categories / lists</li>
            <li>Cloud backup and restore</li>
            <li>Calendar sync &amp; reminders</li>
            <li>Premium-only insights</li>
          </ul>
          <a href={BUDGETBABY_APP_STORE_URL} class="btn btn-secondary price-cta" target="_blank" rel="noopener" data-event="app_store_click">Get BudgetBaby</a>
        </div>

        <div class="price-card price-card--featured">
          <div class="price-card-badge">Best value</div>
          <div class="price-card-head">
            <span class="price-tier">Annual</span>
            <div class="price-amt-row">
              <span class="price-amt">$69.99</span>
              <span class="price-period">/year</span>
            </div>
            <span class="price-save">Save 27% vs monthly · per app</span>
          </div>
          <ul class="price-features">
            <li>Everything in Monthly</li>
            <li>Two months free</li>
            <li>Priority support</li>
            <li>Early access to new features</li>
            <li>Free trial on first sign-up</li>
          </ul>
          <a href={BUDGETBABY_APP_STORE_URL} class="btn btn-primary price-cta" target="_blank" rel="noopener" data-event="app_store_click">Start free trial</a>
        </div>
      </div>

      <p class="pricing-fineprint">
        Free tier is a real app with core features for both BudgetBaby and PantryBaby. Premium unlocks the extras per-app. Cancel anytime from iOS Settings. Prices shown in USD; localised prices may vary slightly by store region.
      </p>
    </div>
  </section>

  <!-- ── Download anchor ──────────────────────────────────────────────── -->
  <section class="waitlist-section section-sm">
    <div class="container-sm">
      <div id="hero-waitlist" class="waitlist-anchor" style="text-align: center;">
        <h2 style="margin-bottom: var(--space-3);">BudgetBaby is live on the App Store.</h2>
        <p style="color: var(--text-secondary); margin-bottom: var(--space-6);">Free to use, with optional Premium. iPhone, iOS 17 and up.</p>
        <a href={BUDGETBABY_APP_STORE_URL} class="btn btn-primary btn-lg" target="_blank" rel="noopener" data-event="app_store_click">
          Download on the App Store
        </a>
      </div>
    </div>
  </section>

  <CTASection
    heading="Ready to budget with confidence?"
    subheading="BudgetBaby is live on the App Store. Free to use, with optional Premium."
    primaryLabel="Download on the App Store"
    primaryHref={BUDGETBABY_APP_STORE_URL}
    secondaryLabel="How BudgetBaby works"
    secondaryHref="/budgetbaby"
  />
</Layout>

<style>
  /* ── Hero ─────────────────────────────────────────────────────────── */
  .hero {
    padding-top: calc(var(--space-16) + var(--space-2));
    padding-bottom: var(--space-20);
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1.05fr 1fr;
    gap: var(--space-16);
    align-items: center;
  }

  .hero-copy { max-width: 560px; }

  /* Lineage chip. Sits above the BudgetBaby brand row so visitors who want
     to resolve "who made this" can see the chain (studio + holding company)
     without having to go to /about. */
  .lineage-chip {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 4px 10px;
    border-radius: 999px;
    border: 1px solid rgba(122, 32, 48, 0.22);
    background: rgba(122, 32, 48, 0.05);
    font-size: 0.7rem;
    font-weight: 500;
    letter-spacing: 0.02em;
    text-decoration: none;
    margin-bottom: var(--space-4);
    transition: border-color 140ms ease, background 140ms ease;
  }
  .lineage-chip:hover {
    border-color: rgba(122, 32, 48, 0.4);
    background: rgba(122, 32, 48, 0.08);
  }
  .lineage-chip-faint { color: var(--text-secondary, #6b7280); }
  .lineage-chip-strong { color: #7A2030; font-weight: 600; }
  .lineage-chip-sep { color: var(--text-tertiary, #9ca3af); }

  .brand-row {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    margin-bottom: var(--space-6);
    flex-wrap: wrap;
  }

  .brand-glyph {
    border-radius: 22%;
    box-shadow: var(--shadow-sm);
  }

  .brand-pill {
    font-size: var(--text-lg);
    font-weight: var(--weight-semibold);
    letter-spacing: -0.01em;
    color: var(--text-primary);
  }

  .brand-status {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    margin-left: var(--space-2);
    padding: 4px 10px;
    background: rgba(255, 255, 255, 0.7);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-full);
    font-size: 11px;
    font-weight: var(--weight-medium);
    color: var(--text-muted);
    letter-spacing: 0.02em;
    text-decoration: none;
    transition: background 140ms ease, color 140ms ease, border-color 140ms ease;
  }
  a.brand-status:hover {
    background: rgba(255, 255, 255, 0.9);
    border-color: var(--coral-500);
    color: var(--text-primary);
  }
  .brand-status-dot {
    width: 6px; height: 6px;
    border-radius: 999px;
    background: var(--coral-500);
    box-shadow: 0 0 0 3px rgba(242, 94, 114, 0.16);
  }

  .hero-title {
    font-size: clamp(2.5rem, 5.5vw, 4.25rem);
    line-height: 1.04;
    color: var(--text-primary);
    margin-bottom: var(--space-5);
  }

  .hero-sub {
    font-size: var(--text-lg);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    max-width: 48ch;
    margin-bottom: var(--space-6);
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
    margin-top: var(--space-2);
    margin-bottom: var(--space-3);
  }

  /* Honest status line under the waitlist CTA. Sets expectations so
     visitors aren't surprised the App Store link isn't live yet. */
  .hero-launch-note {
    font-size: 0.85rem;
    color: var(--text-secondary, #6b7280);
    margin: 0 0 var(--space-6);
    max-width: 44ch;
  }

  /* ── Hero trust signals ── */
  /* Honest hero facts row. Three short pills, no fabricated stars or
     avatars. The current page is for an app in App Review with no
     public users yet; faked social proof is the wrong move. */
  .hero-facts {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    align-items: center;
    gap: var(--space-2);
    flex-wrap: wrap;
  }
  .hero-facts li {
    font-size: var(--text-xs);
    color: var(--text-secondary);
    padding: 4px 10px;
    border-radius: 999px;
    background: rgba(15, 14, 20, 0.04);
    border: 1px solid rgba(15, 14, 20, 0.06);
    white-space: nowrap;
  }

  /* ── Phone stage ─────────────────────────────────────────────────── */
  .hero-visual {
    position: relative;
    display: flex;
    justify-content: center;
  }

  .phone-stage {
    position: relative;
    width: 100%;
    max-width: 520px;
    aspect-ratio: 5 / 4;
    isolation: isolate;
  }

  .phone-glow {
    position: absolute;
    inset: -10%;
    background:
      radial-gradient(circle at 65% 50%, rgba(255, 184, 199, 0.35) 0%, transparent 55%),
      radial-gradient(circle at 30% 70%, rgba(255, 217, 191, 0.30) 0%, transparent 55%);
    filter: blur(8px);
    z-index: -2;
  }

  /* Decorative shapes sit BEHIND the phones (z-index: -1) and at reduced
     opacity so they read as backdrop, not as competing visuals. The phones
     themselves carry strong shadows for separation against the light page. */
  .phone-block {
    position: absolute;
    border-radius: 50%;
    z-index: -1;
  }
  .phone-block--peach {
    width: 150px; height: 150px;
    right: -10%; bottom: 4%;
    background: var(--peach-200);
    opacity: 0.55;
  }
  .phone-block--blush {
    width: 70px; height: 70px;
    right: 16%; top: -4%;
    background: var(--blush-300);
    opacity: 0.55;
  }

  .phone-slot {
    position: absolute;
  }
  .phone-slot--back  { left: 6%;  top: 8%;  z-index: 1; }
  .phone-slot--front { right: 4%; top: 0;   z-index: 2; }

  /* ── Dark feature strip ─────────────────────────────────────────── */
  .feature-strip { padding-block: var(--space-16); }

  .feature-strip-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-3);
  }

  .feature-tile {
    border: 1px solid rgba(255, 247, 239, 0.08);
    border-radius: var(--radius-xl);
    padding: var(--space-6);
    background: rgba(255, 247, 239, 0.025);
    transition: border-color 0.18s ease, transform 0.18s ease;
  }
  .feature-tile:hover {
    border-color: rgba(255, 184, 199, 0.30);
    transform: translateY(-2px);
  }
  .feature-tile-icon {
    width: 40px; height: 40px;
    border-radius: 12px;
    background: linear-gradient(180deg, rgba(255, 207, 216, 0.16), rgba(255, 207, 216, 0.06));
    border: 1px solid rgba(255, 207, 216, 0.18);
    color: var(--blush-300);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    margin-bottom: var(--space-4);
  }
  .feature-tile h3 {
    color: #FFF7EF;
    font-size: var(--text-base);
    margin-bottom: var(--space-2);
    font-weight: var(--weight-semibold);
  }
  .feature-tile p {
    color: rgba(255, 247, 239, 0.65);
    font-size: var(--text-sm);
    line-height: var(--leading-relaxed);
    margin: 0;
  }

  .dark-trust {
    margin-top: var(--space-12);
    padding-top: var(--space-8);
    text-align: center;
  }
  .dark-trust-rule {
    width: 36px;
    height: 1px;
    background: rgba(255, 247, 239, 0.20);
    margin: 0 auto var(--space-6);
  }
  .dark-trust-line {
    font-size: var(--text-base);
    color: rgba(255, 247, 239, 0.78);
    line-height: var(--leading-relaxed);
    max-width: 60ch;
    margin: 0 auto;
  }
  .dark-trust-eyebrow {
    display: block;
    font-size: 10px;
    font-weight: var(--weight-semibold);
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--peach-200);
    margin-bottom: var(--space-3);
  }
  .dark-trust-line a {
    color: var(--peach-200);
    margin-left: var(--space-2);
    font-weight: var(--weight-medium);
    white-space: nowrap;
  }
  .dark-trust-line a:hover { color: #FFF7EF; }

  /* ── Apps section ─────────────────────────────────────────── */
  .apps-section { padding-block: var(--space-20) var(--space-8); }

  .apps-section-head {
    margin-bottom: var(--space-10);
    max-width: 56ch;
  }
  .apps-section-head .eyebrow {
    display: inline-block;
    font-size: var(--text-xs);
    font-weight: var(--weight-semibold);
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--accent-deep);
    margin-bottom: var(--space-3);
  }
  .apps-section-head h2 {
    font-size: clamp(var(--text-3xl), 4vw, var(--text-4xl));
    margin-bottom: var(--space-3);
    color: var(--text-primary);
  }
  .apps-section-head p {
    font-size: var(--text-base);
    color: var(--text-secondary);
  }

  .apps-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-5);
  }

  .app-card {
    background: #FFFFFF;
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-xl);
    padding: var(--space-6);
    text-decoration: none;
    color: inherit;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
    box-shadow: var(--shadow-sm);
  }
  .app-card:hover {
    transform: translateY(-3px);
    box-shadow: var(--shadow-md);
    border-color: var(--glass-border-strong);
  }
  .app-card--featured {
    background: linear-gradient(180deg, var(--cream-50) 0%, #FFFFFF 100%);
    border-color: var(--accent-border);
  }

  .app-card-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-3);
    margin-bottom: var(--space-2);
  }

  .app-icon-future {
    width: 48px; height: 48px;
    border-radius: 12px;
    background: var(--cream-200);
    color: var(--text-muted);
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }

  .app-status {
    font-size: 11px;
    font-weight: var(--weight-semibold);
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    text-transform: uppercase;
    letter-spacing: 0.10em;
  }
  .app-status--soon   { color: var(--accent-deep); }
  .app-status--build  { color: var(--text-secondary); }
  .app-status--future { color: var(--text-faint); }

  .status-dot {
    width: 7px; height: 7px;
    border-radius: 999px;
    flex-shrink: 0;
  }
  .status-dot--soon {
    background: var(--coral-500);
    box-shadow: 0 0 0 3px rgba(242, 94, 114, 0.16);
  }
  .status-dot--build {
    background: transparent;
    box-shadow: inset 0 0 0 1.5px var(--text-muted);
  }
  .status-dot--future {
    background: var(--text-faint);
    border-radius: 1px;
    width: 6px; height: 6px;
  }

  .app-card h3 {
    font-size: var(--text-xl);
    color: var(--text-primary);
    font-weight: var(--weight-semibold);
  }
  .app-card p {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    flex: 1;
  }
  .app-card-link {
    font-size: var(--text-sm);
    font-weight: var(--weight-semibold);
    color: var(--accent-deep);
    margin-top: var(--space-2);
  }

  /* ── Pricing section ──────────────────────────────────────────── */
  .pricing-section { padding-block: var(--space-16) var(--space-20); }

  .pricing-head {
    margin-bottom: var(--space-12);
  }
  .pricing-head h2 {
    font-size: clamp(var(--text-3xl), 4vw, var(--text-4xl));
    margin-bottom: var(--space-3);
  }
  .pricing-head p {
    font-size: var(--text-lg);
    color: var(--text-secondary);
    max-width: 48ch;
    margin-inline: auto;
  }
  .pricing-head .eyebrow {
    display: inline-block;
    font-size: var(--text-xs);
    font-weight: var(--weight-semibold);
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--accent-deep);
    margin-bottom: var(--space-3);
  }

  .pricing-cards {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-6);
    max-width: 700px;
    margin-inline: auto;
  }

  .price-card {
    background: #FFFFFF;
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-xl);
    padding: var(--space-8) var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-5);
    box-shadow: var(--shadow-sm);
    position: relative;
  }

  .price-card--featured {
    background: linear-gradient(180deg, #FFFFFF 0%, #FFF6F2 100%);
    border-color: var(--accent-border);
    box-shadow: var(--shadow-md);
  }

  .price-card-badge {
    position: absolute;
    top: -12px;
    left: 50%;
    transform: translateX(-50%);
    background: linear-gradient(180deg, var(--coral-500) 0%, var(--coral-600) 100%);
    color: #fff;
    font-size: 11px;
    font-weight: var(--weight-semibold);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    padding: 4px 14px;
    border-radius: var(--radius-full);
    white-space: nowrap;
    box-shadow: 0 4px 12px rgba(242, 94, 114, 0.28);
  }

  .price-card-head {
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }

  .price-tier {
    font-size: var(--text-base);
    font-weight: var(--weight-semibold);
    color: var(--accent-deep);
  }

  .price-amt-row {
    display: flex;
    align-items: baseline;
    gap: 4px;
  }

  .price-amt {
    font-size: var(--text-4xl);
    font-weight: var(--weight-bold);
    color: var(--text-primary);
    letter-spacing: -0.03em;
    line-height: 1;
  }

  .price-period {
    font-size: var(--text-sm);
    color: var(--text-muted);
    font-weight: var(--weight-regular);
  }

  .price-save {
    font-size: var(--text-xs);
    color: var(--accent-deep);
    font-weight: var(--weight-semibold);
    background: var(--accent-dim);
    padding: 2px 8px;
    border-radius: var(--radius-full);
    align-self: flex-start;
    margin-top: var(--space-1);
  }

  .price-features {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    flex: 1;
  }
  .price-features li {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    padding-left: 20px;
    position: relative;
    line-height: var(--leading-relaxed);
  }
  .price-features li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 5px;
    width: 12px;
    height: 12px;
    background-color: var(--accent);
    -webkit-mask-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='black' stroke-width='3' stroke-linecap='round' stroke-linejoin='round'><polyline points='20 6 9 17 4 12'/></svg>");
    mask-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='black' stroke-width='3' stroke-linecap='round' stroke-linejoin='round'><polyline points='20 6 9 17 4 12'/></svg>");
    -webkit-mask-repeat: no-repeat;
    mask-repeat: no-repeat;
  }

  .price-cta { width: 100%; }

  .pricing-fineprint {
    text-align: center;
    font-size: var(--text-xs);
    color: var(--text-muted);
    margin-top: var(--space-8);
    max-width: 56ch;
    margin-inline: auto;
  }

  /* ── Waitlist ─────────────────────────────────────────────────── */
  .waitlist-section { padding-block: var(--space-12); }
  .waitlist-anchor { scroll-margin-top: 6rem; }

  /* ── Responsive ───────────────────────────────────────────────── */
  @media (max-width: 960px) {
    .hero-grid { grid-template-columns: 1fr; gap: var(--space-12); }
    .hero-visual { order: -1; max-width: 460px; margin-inline: auto; width: 100%; }
    .feature-strip-grid { grid-template-columns: repeat(2, 1fr); }
    .apps-grid { grid-template-columns: 1fr; }
    .pricing-cards { grid-template-columns: 1fr; max-width: 420px; }
  }

  @media (max-width: 600px) {
    .hero-actions { flex-direction: column; align-items: stretch; }
    .hero-actions .btn { justify-content: center; width: 100%; }
    .feature-strip-grid { grid-template-columns: 1fr; }
    .pricing-cards { grid-template-columns: 1fr; }

    .phone-stage {
      aspect-ratio: 3 / 4;
      max-width: 320px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-inline: auto;
    }
    .phone-slot--back  { display: none; }
    .phone-slot--front {
      position: static;
      transform: none;
    }
    .hero-title { font-size: clamp(2rem, 9vw, 2.75rem); }
    .hero-sub { font-size: var(--text-base); }

    .brand-row { gap: var(--space-2); }
    .brand-status { margin-left: 0; }

    .hero-facts { gap: var(--space-1); }
    .hero-facts li { font-size: 11px; padding: 3px 8px; }
  }
</style>
